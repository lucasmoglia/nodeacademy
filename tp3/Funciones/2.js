/**
 * Desarrollar una función que reciba un dato de tipo bool. Si el parámetro es verdadero retorna “:)”, si no, “:(“.
        Ej. sonrieSi(true) -> “:)”
 */

 function sonrieSi(_bool){
     if(_bool){
         return ':)';
     } else {
         return ':(';
     }
 }