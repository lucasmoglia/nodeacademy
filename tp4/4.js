/**
 * Diseñar un objeto semáforo que tenga entre otros atributos (a libre elección), 
 * - estado (string) y 
 * - colores (array de strings: 'rojo', 'amarillo', 'verde').
 */

 const semaforo = {
     estado: 'avanzar',
     colores: ['rojo', 'amarillo', 'verde'],
 }