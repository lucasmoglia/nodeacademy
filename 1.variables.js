/**
 * Las variables son espacios en memoria, en los cuales podemos almacenar cosas!
 * Para usar una variable, tenemos que:
 * - Declararla
 * - Asignarla (opcional)
 * - Usarla (mostrar el valor)
 * - Podemos cambiar el valor!
 * - Usarla (mostrar el valor y comprobar que cambió)
 * @see 0.common.js Para ver como imprimir valores en la consola
 */

 // Declaracion
 let variable1;

 // Asignarla
 variable1 = 2;

 // Usarla (tiene que imprimir 2)
 console.log(variable1);

 // Cambiar el valor
 variable1 = 3;

 // Usarla (tiene que imprimir 3)
 console.log(variable1);